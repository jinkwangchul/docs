MASTER_SPEC_CANONICAL_2026-07-17_CURRENT_STATE_RESYNC

Status:
- Current State Canonical generated by Canonical Current State Resync.
- This document records the current GitHub baseline and the current production
  implementation state only.
- Historical branch state, finished change-set status, and stale next-task
  notes are not current contract.
- Older Canonical, Changelog, and Work Resume files are preserved in archive as
  historical evidence.

Baseline:
- docs repository branch: main
- docs repository HEAD: 09ffe28afdc5570e7f2b878c314da9b2f1fa33d7
- kiwoom_auto repository branch: master
- kiwoom_auto repository HEAD: 4eaefa77e06e0fc31990b2ba1a25eaa2ec4c87c6

1. Source of Truth Roles

- AGENT_WORKFLOW_REFERENCE is the only AI operating-rule source.
- Reference Index is an index only.
- Latest Canonical is the current technical contract.
- Latest Work Resume is the current recovery and next-start context.
- Latest Changelog records Canonical changes.
- Git history preserves historical implementation and document evolution.

2. Current Production Call Flow

Application entry:

gui_main.py
-> gui_windows.MainWindow
-> AutoTradeSettingWindow for automatic trading setup and operation controls.

Current automatic-trading timer path on master:

AutoTradeSettingWindow
-> on_time_policy_timer_tick
-> auto_trade_on_time_policy_timer_tick
-> startup recovery session check
-> operation/time policy recalculation
-> probe_selected_routine_once
-> routine signal queue

Current consumer condition on master:

auto_trade_on_time_policy_timer_tick calls consume_pending_routine_signals_dry_run
only when auto_trade_signal_probe_only_active(window) is true.

Therefore normal real automatic trading state:

- status = RUNNING
- trade_enabled = true
- real_trade_enabled = true
- signal_probe_only = false

does not enter the consumer from the timer path on current master.

3. Current Manual Production Execution Chain

The following manual GUI production chain exists on current master:

PENDING routine signal
-> consume_pending_routine_signals_dry_run
-> order_queue candidate creation
-> approval
-> Operation Policy Gate
-> EXECUTABLE
-> manual Execution Enable
-> REAL_READY
-> manual Execution Preview
-> runtime file initialization / runtime commit path
-> queue commit
-> ORDER_QUEUED
-> manual SendOrder flow
-> Final Send Gate
-> dispatch claim
-> execute_claimed_send_order
-> Kiwoom SendOrder callable boundary

The manual SendOrder path keeps the existing safety boundaries:

- order status must be ORDER_QUEUED
- SendOrder environment must be ready
- adapter contract must be ready
- queue snapshot must not change unexpectedly
- Final Send Gate must pass
- dispatch claim must succeed before SendOrder callable execution

4. Current Chejan / Fill / Position / Broker Holdings Contracts

The current master keeps the following runtime evidence contracts:

- Kiwoom Chejan normalization preserves original_order_no from FID 904.
- Chejan recorder links cancel/modify request records with original orders
  through original_order_no and broker identity evidence.
- Cancel/modify lifecycle updates are driven by Chejan evidence, not by
  SendOrder return value alone.
- Fill recording and Position update use runtime evidence paths.
- gubun=1 broker holdings events write broker_holdings snapshot evidence.
- Broker holdings do not automatically modify internal positions.
- Broker holdings and internal positions may be compared for reconciliation.

5. Current Startup Recovery / Operator Recovery Contracts

The current master includes startup recovery and operator recovery capabilities:

- Startup Recovery blocks automatic trading before session approval.
- Runtime evidence snapshot changes invalidate prior startup approval.
- Startup Recovery does not resend orders.
- Startup Recovery does not erase runtime evidence.
- Operator reconciliation uses saved runtime evidence and existing recorders.
- Safe reprocessing is evidence-based and does not call Broker API or SendOrder.

6. Current UI Contract State

The AutoTradeSettingWindow control set is restored on current master:

- User-facing confirmed controls remain visible.
- Internal pipeline controls are not exposed as normal operator controls.
- Production call graph and runtime mutation contracts are not changed by that
  UI cleanup.

MainWindow remains the current operational entry screen. MainWindow layout
redesign proposals are not Canonical implementation contract.

7. Current Baseline Fixes

The current master includes the buy expression baseline fix:

- routines/지표추종매매/rules.json
- indicator_follow_ui_state.state.basic.buy_signal_expr_line
- current baseline value: A and B and C and D

RESTORE_TEST_BUY_EXPR is historical test contamination and is not the current
production baseline value.

8. Current Production Gap

The earliest user-visible production gap on current master is:

Normal real automatic trading signals do not progress from timer-generated
routine signal evidence into the order candidate consumer because the timer
consumer path is guarded by signal_probe_only state.

Immediate implementation start point:

gui_auto_trade_timer.auto_trade_on_time_policy_timer_tick
-> consumer entry condition
-> allow normal real automatic trading state to enter the existing consumer
   without changing signal_probe_only semantics.

Required safety boundaries for the next implementation:

- Do not set signal_probe_only=true for real trading.
- Do not mix probe-only and real automatic trading meanings.
- Do not bypass Startup Recovery.
- Do not bypass review_required, emergency stop, duplicate protection,
  order locks, Final Send Gate, or SendOrder callable checks.
- Do not call real Kiwoom SendOrder during validation without explicit
  operator approval.

9. Stale State Removed From Current Contract

The following are not current Source of Truth:

- old feature branch baseline names
- stale local feature commits
- stale OPEN/CLOSED change-set status
- already completed work recorded as next work
- local-only validation state not present on current master
- historical PR or merge process details

10. Validation Evidence Used For This Resync

- GitHub docs/main HEAD was checked directly.
- GitHub kiwoom_auto/master HEAD was checked directly.
- Current master code was inspected through origin/master without changing code
  or runtime files.
- Existing stale Current documents were moved to archive instead of deleted.
