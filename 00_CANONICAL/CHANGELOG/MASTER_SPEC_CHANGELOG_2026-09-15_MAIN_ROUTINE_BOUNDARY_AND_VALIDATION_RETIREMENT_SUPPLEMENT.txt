MASTER SPEC CHANGELOG
2026-09-15 MAIN-ROUTINE BOUNDARY AND VALIDATION RETIREMENT SUPPLEMENT

Status: VERIFIED SUPPLEMENTAL CHANGE RECORD
Code branch: feat/restore-auto-trade-setting-ui
Base code HEAD: a45fd5c7b01cf36f528bcc753c414155eeeee37c
Verified code HEAD: ee40370402aaee5bf642cda3e17b9bf7a53eb15b

======================================================================
1. VERIFIED CODE COMMITS
======================================================================

- 8d35be3 Normalize main and routine dependency boundaries
  Removed an unused Indicator-Follow import from common signal probing, moved
  effective running-budget projection into Main facts capture, and removed the
  Routine's direct running_budget_adjustment dependency without changing the
  continuation result contract.

- 499b006 Retire legacy validation and isolate signal validation V2
  Removed the approved V1 flow/chart/button/binders, transferred the required
  canvas behavior to V2, moved SELL selection validation to Routine ownership,
  made V2 optional at the Routine settings boundary, and retained V2/shared
  host, picker, replay, and contract components. The removed 41 V1-only tests
  are retired coverage, not passing tests.

- d44de79 Verify main routine deletion independence
  Replaced an invalid SimpleNamespace/invalid-instance deletion fixture with an
  actual QMainWindow plus temporary repository data. Added real handler/service
  coverage for cancellation, target-only deletion, assigned-stock protection,
  package preservation, and operation with Indicator-Follow absent.

- ee40370 Strengthen routine package absence isolation test
  Corrected the package-absence evidence by running tracked source from a
  temporary executable copy that physically excludes Indicator-Follow and user
  Runtime data. Added source-origin/path assertions, distinct missing-instance
  and missing-package-instance gates, and an injected reverse-dependency control.

======================================================================
2. ERROR CLOSURE
======================================================================

The original failing method was:

  MainRoutineMonitoringDisplayTest.
  test_main_routine_delete_no_confirmation_does_not_delete

The invalid instance id caused the real scope collector to raise ValueError.
The error-report path then passed a types.SimpleNamespace to QMessageBox.warning,
which raised a PyQt TypeError because argument 1 must be QWidget-compatible.
Production deletion code and policy were not changed.

The corrected tests use the actual
MainWindow.delete_routine_instance_from_main_table handler. Confirmation and UI
output are the only mocked boundaries; the scope collector and allowed deletion
service are real against a temporary project root.

======================================================================
3. VERIFICATION RECORD
======================================================================

- At d44de79, 409 distinct relevant tests passed. This remains the earlier run
  record and is not combined with the correction run.
- At ee40370, 26 directly related tests passed: independence, Main deletion core,
  and Routine package/registry suites.
- The physical package-absence and injected reverse-dependency control tests
  both passed; py_compile passed for the changed test module.
- Final new core scenarios: 4/4 passed.
- py_compile: passed for both changed test modules.
- Cancellation: all temporary files byte-identical, service call 0.
- Allowed deletion: only target instance removed; packages and other instance
  preserved.
- Assigned-stock protection: rejected before confirmation, mutation 0.
- The d44de79 package test omitted Indicator-Follow from a temporary registry
  root but still imported Main/common source from the original repository. It
  did not alone prove physical code-package absence.
- The ee40370 test uses `git archive HEAD`, excludes the physical package,
  Runtime/stocks/old instances/log/cache data, runs with the copied root as cwd
  under Python -I, removes PYTHONPATH/PYTHONHOME, and verifies copied-root module
  origins and no target module load.
- In the corrected copy, generic Main imports and independent Routine
  discovery/evaluation/admission passed. Both missing instance and existing
  instance with missing package failed closed with ROUTINE_GATE_UNAVAILABLE.
- Injecting the forbidden target import into only the copied Main module caused
  the expected target-package ModuleNotFoundError, establishing test sensitivity.
- Broker, login, SendOrder, and actual Production data were not exercised.

Existing OOS reproduced and left unchanged:

- SELL protected WIP: 2 failures.
- MockValidationHost UI: 3 failures and 1 error. The error is NO_HOLDING in the
  early-close lifecycle path.

======================================================================
4. PRESERVATION RECORD
======================================================================

No V2 functional/geometry/Replay file changed in d44de79 or ee40370. September 14 V2
marker, Tooltip, axis, cycle, aggregate return, settings apply, and Replay
Phase 2-4 contracts remain unchanged. September 14 performance figures were not
remeasured in this work.

Code protected WIP remains 303 paths. The within-task reproducible protected-WIP
SHA-256 stayed:

  f16b34b0d44b62099f3900a152db4e8c8b61f6642e25ab69dce703af4ced37d9

The historical 61ce8a and b3f597 fingerprints are not declared comparable
because their exact byte procedures could not be recovered.

======================================================================
5. DOCUMENTATION CHANGE
======================================================================

The original documentation commit added only:

- MASTER_SPEC_CANONICAL_2026-09-15_MAIN_ROUTINE_BOUNDARY_AND_VALIDATION_RETIREMENT_SUPPLEMENT.txt
- MASTER_SPEC_CHANGELOG_2026-09-15_MAIN_ROUTINE_BOUNDARY_AND_VALIDATION_RETIREMENT_SUPPLEMENT.txt
- one separable Current Primary Owners row in 00_REFERENCE_INDEX.md.

This correction modifies only the existing Canonical and Changelog above. The
Reference Index is unchanged, including its protected unstaged WIP. No past
Freeze is modified. This is not a Project Freeze, Work Resume, or next session
startup prompt.
