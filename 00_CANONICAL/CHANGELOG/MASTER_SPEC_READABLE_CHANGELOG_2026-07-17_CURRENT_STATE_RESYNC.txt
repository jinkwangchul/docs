MASTER_SPEC_READABLE_CHANGELOG_2026-07-17_CURRENT_STATE_RESYNC

Canonical Current State Resync is complete.

What changed:
- The current project baseline now uses GitHub docs/main and
  kiwoom_auto/master, not stale feature branch progress.
- Current Canonical and Work Resume now describe what exists on current master.
- Historical progress documents were archived instead of deleted.

What is now clear:
- Manual execution can proceed through ORDER_QUEUED, Final Send Gate,
  dispatch claim, and execute_claimed_send_order.
- Routine signals can be generated by the GUI timer.
- The current master timer only consumes pending routine signals when
  signal_probe_only is active.
- Real automatic trading uses signal_probe_only=false, so the next production
  gap is the timer consumer entry condition for normal real trading.

What was removed from current state:
- stale feature branch baseline
- stale local commit baseline
- historical pending change-set wording
- completed work listed as next work
- local-only validation state not present on current master

Next implementation:
- Start at gui_auto_trade_timer.auto_trade_on_time_policy_timer_tick.
- Connect normal real automatic trading state to the existing pending signal
  consumer without changing signal_probe_only meaning.

Safety:
- Code was not changed.
- Runtime files were not changed.
- Historical documents were preserved in archive.
