# AGENT_WORKFLOW_REFERENCE

Document role:
- This file is the single Source of Truth for AI operating rules.
- All AI agents working on this project must follow this file before README, session protocols, older operation manuals, Work Resume notes, or historical Canonical text.
- Other documents provide project contracts, indexes, evidence, or recovery context. They do not define AI operating procedure unless this file explicitly delegates that role.

## 1. Authority And Interpretation

Priority order:

1. Latest explicit user instruction
2. `00_CANONICAL/REFERENCE/AGENT_WORKFLOW_REFERENCE.txt`
3. Latest Official Canonical for technical project contracts
4. Latest Work Resume for recovery context
5. Actual local code, Runtime files, logs, and execution evidence
6. Reference Index as a document index only
7. Historical and archived files only as evidence

Rules:
- Latest explicit user instruction always overrides this file.
- The user owns project concepts, rules, policies, states, terms, UI contracts, data contracts, and structure contracts.
- AI has no authority to create, change, replace, expand, shrink, reinterpret, or delete project contracts without explicit user approval.
- AI reasoning is only a tool for finding evidence. It is not implementation authority.
- If implementation and contract conflict, fix the implementation. Do not rewrite the contract to fit AI interpretation.
- User-unapproved contract change is project contamination.

Close Process Reference Rule:
- For 조기마감, 자동마감, 청산정책, 마감정책, and 즉시청산, use
  `00_CANONICAL/CURRENT/MASTER_SPEC_CANONICAL_2026-08-01_CLOSE_PROCESS_OPERATION_CONTRACT.txt`
  as the first technical Source of Truth.
- Archive and Legacy close-process material is historical evidence only and
  must not be used to decide current operating contracts.
- If Archive or Legacy material conflicts with Current Canonical, Current
  Canonical wins.
- If UI terminology and document terminology conflict, the confirmed UI term
  wins.

GUI State And Context Menu Reference Rule:
- For REVIEW_REQUIRED, data inconsistency, protected-row display, situation
  signal color, status/situation sorting, and AutoTradeSettingWindow
  routine-tree `종목등록` context-menu behavior, use
  `00_CANONICAL/CURRENT/MASTER_SPEC_CANONICAL_2026-08-01_GUI_STATE_AND_STOCK_REGISTER_CONTEXT_CONTRACT.txt`
  as the first technical Source of Truth.
- When the same action label appears in multiple GUI screens, first trace the
  actual visible event path and handler used by the user. Fixing another screen
  with the same label is not a valid implementation.
- Tests must target the actual visible GUI handler for the requested screen,
  not only a similarly named helper or another window's context menu.

## 2. Project Identity

Automatic Trading Project:

- Local Repository:
  `C:\Users\JIN KWANG CHUL\Documents\kiwoom_auto`
- GitHub Repository:
  `https://github.com/jinkwangchul/kiwoom_auto`
- Default Remote:
  `origin`

Documentation Repository:

- Local Repository:
  `C:\Users\JIN KWANG CHUL\Documents\docs`
- GitHub Repository:
  `https://github.com/jinkwangchul/docs`
- Default Remote:
  `origin`

Repository verification rule:
- At new-session startup, first determine the execution environment.
- If local Git command execution is available, run `git remote -v` for each repository that will be used.
- If local Git command execution is not available, do not invent or infer local Git command results.
- Verify the remote URL against the corresponding Project Identity GitHub Repository only from evidence available in the current execution environment.
- Do not start project work if an available repository remote does not match Project Identity.
- Report a confirmed mismatch, or report that local verification is unavailable, and wait for user instruction instead of guessing or switching repositories.

Execution Environment Policy:
- Before starting project work, the agent must identify what repository evidence
  it can actually access in the current environment.
- Report this first:

```text
Execution Environment

- Local Repository Access: Available / Not Available
- Local Git Command Execution: Available / Not Available
- GitHub Repository Access: Available / Not Available
```

Environment A: local Git execution available.
- Examples: Codex CLI, local Codex, VSCode, or an agent with direct access to
  the user's local repositories.
- Actually run, when relevant:

```text
git remote -v
git branch --show-current
git rev-parse HEAD
git status -sb
git diff --stat
```

- Use the command results as evidence.

Environment B: local Git execution unavailable.
- Examples: ChatGPT Web, GitHub Connector only, or an agent without local
  repository access.
- Do not guess local repository state.
- Do not fabricate `git remote`, `git status`, `git diff`, branch, commit, or
  dirty-state results.
- Clearly separate information that is visible through GitHub from information
  that is not locally verifiable.
- If local state is required, ask the user to run the local commands or move the
  work to a local-execution agent.

## 3. Work Purpose

The project goal is working automatic trading software, not document growth, structure growth, or Git activity.

Every task starts with one fixed problem statement or Big Feature Goal.

Default work flow:

1. Define the actual problem
2. Trace the real call path
3. Confirm the actual stop point
4. Make the smallest necessary change
5. Verify through execution, tests, Runtime evidence, or logs
6. Report only the result and remaining real stop points

Do not change the problem during the task. New findings are reported separately unless the user explicitly expands scope.

## 4. Implementation First

Implementation First means actual user-facing project behavior has priority over process work.

Default priority:

1. Real problem resolution
2. Cause confirmation
3. Minimal implementation
4. Verification
5. Local version management
6. Documentation only when requested or at Project Freeze

Forbidden during implementation unless explicitly requested:
- Long repeated structure audits
- Middle-of-work Canonical, Changelog, or Work Resume creation
- Expanding the task into future design
- Treating tests, files, helper functions, or state names as completed user features
- Ending at an internal pipeline stage when the requested user flow remains disconnected

## 5. Big Feature Goal

Work unit is a user-usable feature, not a function, file, helper, adapter, preview, validator, gate, wrapper, executor, or state transition.

A Big Feature is complete only when the user can use it in the actual production flow or when the exact remaining stop point is proven.

During a Big Feature:
- Do not repeatedly split work into smaller featurelets.
- Do not declare completion because one internal layer or test passes.
- Do not select the next feature before the current one reaches a usable state or a proven blocker.
- Small fixes discovered inside the same feature stay inside the same feature.

Completion report must explain the user-visible capability, actual flow, safety boundary, validation result, and remaining stop point if any.

## 6. Change Scope Rule

AI does not perform work outside the current task scope.

Without explicit user approval, do not perform:
- Refactoring
- Optimization
- Naming changes
- Structure changes
- Contract changes
- Test changes
- Documentation changes
- Git work
- Extra feature additions

If an improvement is discovered, report it as:

```text
Additional improvement candidate
- Target:
- Reason:
- Expected impact:
- Recommended separate task:
```

Do not include it in the current work.

## 7. Shared Foundation And Architecture Contamination

Default: no new shared foundation work.

Shared foundation review is allowed only when all are true:
- The current Big Feature cannot be completed with existing structure.
- The blocker is Source of Truth, Canonical Writer, Broker I/O, common storage, external event input, or shared Mutation Boundary.
- Solving it is necessary to avoid bypassing a safety boundary.
- The change can be made with minimal modification to existing structure.

Do not create new layers, writers, policies, previews, gates, adapters, services, wrappers, or executors for convenience, reuse speculation, cleanliness, or future extensibility.

Architecture contamination includes:
- AI-created concepts not approved by the user
- Repeated wrappers around existing responsibility
- Writer or Source of Truth dispersion
- Production call graph changes without contract authority
- Lifecycle owner changes without approval
- Treating unit-test count as production validity

When structural risk is present, report the blocker and minimal option before implementation.

## 8. Source Of Truth And Runtime Safety

Project technical contracts live in the latest Official Canonical. AI operating rules live only in this file.

Runtime safety rules:
- Do not write Runtime files unless the user requested a task that requires it and an existing writer/commit boundary is used.
- Do not directly mutate Queue, Fill, Position, Broker Holdings, locks, state, or rules files unless explicitly authorized.
- Do not create fake Runtime evidence.
- Do not manually create empty JSON to bypass initialization.
- Do not call Kiwoom SendOrder, Broker API, or real order APIs without explicit user approval.
- Do not treat preview, validation, dry-run, or dialog display as commit.
- Do not change Source of Truth or Writer ownership without user approval.

Real side effects must go through the existing canonical writer, service, or commit boundary.

## 9. Session Startup Policy

At the start of a new session, resume implementation from the latest Work Resume. Startup is not document review.

Startup triggers:
- New conversation
- Continuing previous work
- Resuming after a Git baseline change
- User asks to verify current baseline before work

Session Startup Source Resolution Policy:

Before reading project documents or source code, identify the active work environment.

- ChatGPT with GitHub connector: use the GitHub docs repository and GitHub code repository as the remote backup and restoration baseline.
- Local Codex / VSCode: use the local docs repository and local code repository as the working baseline.

Use the source that matches the active environment. Do not mix GitHub connector state and local repository state unless the user explicitly asks for a comparison.
Apply the Execution Environment Policy before reporting repository state. Local Git command results may be reported only when local Git command execution is available.

Startup execution flow:

1. Read the Latest Work Resume only for the previous completion state and next implementation target.
2. Confirm the current Git Baseline (branch and commit) using evidence available in the current execution environment. If local Git command execution is available, run the required local Git commands. If it is not available, report local-only fields as not confirmed instead of guessing.
3. If the next implementation scope is clear from the Work Resume, start implementation immediately.
4. If the Latest Work Resume explicitly states that no next implementation item
   is authorized, do not infer or select a production gap. Wait for the user's
   next implementation instruction.
5. If no explicit next implementation task exists and the Latest Work Resume
   does not prohibit selecting one, identify the earliest user-visible
   production gap in the current verified baseline, briefly report the
   rationale, and begin only within the established Source of Truth.

Reference rules:

6. Refer to the corresponding Latest Canonical section only when needed during implementation.
7. Use Reference Index only to locate a required document.
8. Re-read this file only when operating rules changed, a new operating rule must be applied, or the user asks.

Forbidden:
- Broad structure audit without need
- Re-reading AGENT, Reference Index, full Canonical, historical documents, or broad project structure by default
- Adding startup checks inferred from general development or Git practices
- Treating old Work Resume next steps as current user instruction
- Starting implementation before the fixed problem and allowed scope are known

### PR History Exclusion Policy

Session Startup and restoration after Project Freeze must not use Pull Request
or merge history as the working baseline.

The following are not Startup Sources:
- Pull Request lists
- closed or merged Pull Requests
- merge history
- the latest state of the GitHub default branch such as `master` or `main`
- recent merge commits
- past feature branches
- past review records

These may be inspected only when the user explicitly requests them. Even then,
they are reference material and cannot replace the approved restoration
baseline.

Startup Source priority is:
1. Latest user instruction
2. `AGENT_WORKFLOW_REFERENCE.txt`
3. Latest Official Canonical
4. Latest Work Resume
5. user-specified branch
6. user-specified commit
7. actual Git state available in the current execution environment

Pull Request or merge history cannot replace, reorder, or reinterpret these
sources.

Branch Resolution Rule:
- When the user specifies a branch and commit, that pair is the restoration
  baseline.
- Verify only whether the specified branch exists and whether the specified
  commit exists in the available repository evidence.
- If the remote branch cannot be found, do not infer or replace it from a Pull
  Request, merge history, a recent merge commit, a past feature branch, or the
  default branch.
- If the specified branch or commit cannot be confirmed, report exactly what
  could not be confirmed and stop until the user provides direction.

Work Resume Rule:
- If the Latest Work Resume states that no next implementation item is
  authorized, do not infer a Production Gap.
- Do not inspect Pull Requests or merge history to invent or select the next
  feature.
- Wait for the user's explicit implementation instruction.

GitHub Interpretation Rule:
- GitHub is used for backup, recovery, and baseline verification.
- A recent Pull Request, merge, commit, or default-branch state is not an
  implementation-start authority.

Forbidden:
- Inferring that `master` or `main` is the current baseline because it contains
  work merged after a Pull Request.
- Replacing a user-specified branch with the default branch.
- Interpreting current implementation state from a Pull Request number.
- Finding and starting work from a Pull Request when the Latest Work Resume
  does not authorize a next task.

Reference Index is only an index. It is not an operating policy document.
Follow only the operating procedures defined by this project. Documents support implementation; they must not become a repeated startup gate when the Latest Work Resume already provides the next implementation point.

## 10. Conversation Shutdown / Project Freeze Policy

Conversation Shutdown is Project Freeze, not casual summarization.

Shutdown triggers:
- User says the work is ending
- User says to move to the next conversation or next window
- User says "마감", "작업 종료", "오늘은 여기까지", "Freeze", or equivalent
- User asks to create a next-session baseline

Project Freeze is allowed even though Implementation First normally places documentation after implementation. Freeze work is operational baseline creation, not feature documentation.

Freeze includes, when requested or needed:
- Project state check
- Git status check
- Commit of completed project work
- Push to GitHub as backup and next-session recovery baseline
- New Latest Official Canonical for confirmed project contracts only
- New Latest Work Resume for recovery state
- New Latest Changelog for Canonical changes
- Reference Index update when latest document pointers change
- Final verification that local and remote baselines match

Big Feature Freeze must record:
- Current Big Feature Goal
- Current Big Feature progress
- Completion criteria
- Current blocker or next start point
- Why the next Big Feature must not be selected yet, if the current one is not complete

Freeze is complete only when the next AI can continue the same work from GitHub and the recorded documents without reading the previous conversation.

Freeze must not record ideas, unapproved plans, or discussed UI proposals as Canonical.

## 11. Conversation Closing Standard Procedure

Run this procedure only when the user explicitly says they are moving to the next conversation, opening a new window, closing the window, finishing, wrapping up, or equivalent.

Step 1. Implementation Work Ends

Confirm that the implementation work itself has ended.

This means:
- Code implementation is complete.
- Required tests and verification are complete.
- The completed work scope and excluded scope are known.

Do not create a Next Session Startup Prompt at this step.

Step 2. Run The Codex Work Closing Prompt

Run the user-approved Codex closing prompt.

This step performs the defined close-out work, including as applicable:
- Git status inspection
- Commit
- Push
- Canonical update
- Changelog update
- Work Resume update
- Other close-out work defined by the operation manuals

Do not create a Next Session Startup Prompt during this step.

Step 3. Codex Completion Report

Codex reports the close-out result.

The report must include:
- Commit SHA
- Push result
- Updated documents
- Git status
- Any protected dirty/untracked files that intentionally remain

Do not create a Next Session Startup Prompt in the completion report unless Steps 2 through 4 have already completed.

Step 4. Result Verification

Verify the close-out result before declaring Conversation Closing complete.

Required checks:
- Commit SHA
- Branch
- Push success or exact push failure reason
- Git status
- Canonical creation or update status
- Work Resume creation or update status
- Changelog creation or update status
- Protected dirty/untracked files remain protected

Conversation Closing is complete only after these checks pass or an unresolved blocker is explicitly reported.

Step 5. Create The Next Session Startup Prompt

Create the Next Session Startup Prompt only after Steps 2 through 4 are complete.

Never create it before close-out commit, push, Canonical, Work Resume, Changelog, and Git status verification are complete.

The prompt must be based on the final:
- Commit
- Branch
- Canonical
- Work Resume
- Changelog
- Git status

The Next Session Startup Prompt must be written exactly once.

Forbidden:
- Temporary Next Session Startup Prompt
- Preliminary Next Session Startup Prompt
- Draft Next Session Startup Prompt
- Intermediate Next Session Startup Prompt
- Multiple competing startup prompts

The final prompt must include:
- Environment resolution
- Source of Truth selection
- Conditional repository verification against Project Identity before work starts:
  run `git remote -v`, `git branch --show-current`, `git rev-parse HEAD`,
  `git status -sb`, and `git diff --stat` only when local Git command execution
  is available; otherwise report GitHub-visible information and mark local-only
  verification fields as not confirmed.
- Repository Information block from the Conversation Closing Template
- Latest repository branch and commit
- Dirty or untracked state to preserve
- Recently completed work
- Current implementation location
- The first concrete next implementation task, if one is defined by the Source of Truth

Conversation Closing Template:

The Next Session Startup Prompt must include the following Repository Information
block. Do not omit, abbreviate, move, or manually rewrite this block. Fill Local
Repository, GitHub Repository, and Remote from Project Identity. Fill Branch and
Commit from the verified Conversation Closing result.

```text
==================================================
Repository Information
==================================================

자동매매 프로젝트

Local Repository

<Project Identity에서 자동 참조>

GitHub Repository

<Project Identity에서 자동 참조>

Remote

<Project Identity에서 자동 참조>

현재 Branch

<Conversation Closing 완료 후 실제 Branch>

현재 Commit

<Conversation Closing 완료 후 실제 Commit>

--------------------------------------------------

문서 저장소

Local Repository

<Project Identity에서 자동 참조>

GitHub Repository

<Project Identity에서 자동 참조>

Remote

<Project Identity에서 자동 참조>

현재 Branch

<Conversation Closing 완료 후 실제 Branch>

현재 Commit

<Conversation Closing 완료 후 실제 Commit>
```

Repository Information rules:
- The Repository Information block is mandatory in every Next Session Startup Prompt.
- The block must include both the Automatic Trading Project and the Documentation Repository.
- Local Repository, GitHub Repository, and Remote values must be copied from Project Identity.
- Branch and Commit values must be copied from the final verified Conversation Closing result.
- The prompt author must not manually invent, abbreviate, omit, or relocate repository information.

## 12. Local Git And GitHub

Git is used for:
- Local version management
- Diff and history inspection
- File-level recovery after mistakes
- Final backup commit at stable points

GitHub is used for:
- Remote backup
- AI state restoration
- Project baseline verification
- Remote baseline verification after push
- Next-session recovery reference after the local baseline is pushed
- Collaboration history only when the user explicitly needs it

GitHub is not a development procedure and not a reason to add collaboration workflow.

Rules:
- Do not run Git operations unless requested or necessary for an approved Freeze/Git task.
- Do not use `git add .` or `git add -A`.
- Stage only specified files.
- Do not reset, restore, stash, clean, or delete protected dirty/untracked files unless the user explicitly asks.
- Report branch, local SHA, remote SHA, ahead/behind, status, diff, and untracked files when doing baseline verification.

## 12. Document Roles

Document hierarchy: AI operating rules -> AGENT_WORKFLOW_REFERENCE; technical contract -> Latest Canonical; recovery -> Latest Work Resume; change history -> Latest Changelog; navigation -> Reference Index; repository guide -> README; historical evidence -> 20_ARCHIVE.

Document roles:

- `AGENT_WORKFLOW_REFERENCE`: single AI operating rule source
- `00_REFERENCE_INDEX.md`: index to latest documents
- Canonical: project technical contract
- Work Resume: recovery and next-session context
- Changelog: Canonical change history
- README: repository guide
- Historical / Archive: evidence only

Rules:
- Do not modify old Canonical, old Changelog, or old Work Resume.
- New confirmed contract goes into a new Canonical.
- Canonical only records confirmed implementation or confirmed policy.
- Work Resume records recovery state, current baseline, next start point, and unresolved blockers.
- Changelog records what changed in a Canonical update.
- Intermediate documents are forbidden unless user explicitly asks.

## 13. Prompt Standard

Permanent operating rules are not repeated in every task prompt.

Task prompt should contain only:

1. Purpose
2. Target scope
3. Additional restrictions unique to this task
4. Verification
5. Report format

Default prompt length should stay small:
- Analysis: about 10-20 lines when possible
- Implementation: about 20-40 lines when possible
- Git or document task: about 10-20 lines when possible

Prompt checklist:
- Is this directly related to the current task?
- Does it avoid repeating this operating rule file?
- Does it avoid unrelated Git, test, document, or collaboration workflow instructions?
- Is the purpose one sentence?
- Is the report format not excessive?

## 14. Verification And Reporting

No conclusion without evidence.

Valid evidence:
- Code path
- Runtime value
- state file
- Queue file
- Log
- Test result
- GUI execution result
- Git status or diff for Git tasks

Every change or conclusion must distinguish evidence from inference.

Examples of evidence:
- Test result
- `py_compile` result
- `git status`
- Commit hash
- Runtime file content
- Log line
- Queue or state record

GUI execution-path rule:
- When the same feature name exists in multiple windows, tables, or context menus, trace the actual visible widget signal, slot, condition branch, and final menu/action list before changing code.
- A test against a similarly named helper, callback, or different window is not evidence that the user-visible GUI path was fixed.

If evidence is missing, report "not confirmed".

Report order for bug/feature work:

1. Problem definition
2. Call chain
3. Actual stop point
4. Cause
5. Evidence
6. Change location
7. Change summary
8. Verification result
9. Remaining issue

Do not over-report file and function names before explaining actual user-facing behavior.

## 15. Prohibited Defaults

Unless explicitly requested, do not:
- Create new project concepts
- Create new statuses
- Create new UI contracts
- Create new Source of Truth
- Create new writer or mutation boundary
- Add hidden Runtime writes
- Connect real SendOrder
- Use Broker API
- Generate new Canonical, Changelog, or Work Resume during implementation
- Perform Git work
- Modify unrelated files
- Expand a bug task into architecture cleanup
- Turn analysis into implementation

## 16. Final Principle

The AI's job is to make the current real project work under the user's contracts.

When in doubt:

1. Preserve the contract
2. Trace the real execution path
3. Fix only the proven blocker
4. Verify with real evidence
5. Report the next real blocker
